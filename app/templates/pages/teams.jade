.container
  .content
    h1 Contestants
    .teams
      .container
          each team in faction
            .row
              each member in team
                .col-sm-6
                  .team.member
                    each image, index in member.images
                      if (index === 0)
                        a(href="#{image}", data-lightbox='contestants-asmi-#{member.name}', data-title='#{member.name}')
                          img(src="#{image}")
                       else
                         a(href="#{image}", data-lightbox='contestants-asmi-#{member.name}', data-title='#{member.name}')
                  .team.member.name
                    h4 #{member.name}
                  .team.member.description
                    p #{member.description}
